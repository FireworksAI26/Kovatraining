[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "kova-training"
version = "0.1.0"
description = "Fail-closed controls for the KovaDev training campaign"
requires-python = ">=3.11"
dependencies = [
  "daytona==0.200.1",
]

[project.optional-dependencies]
dev = [
  "mypy==1.17.1",
  "pyyaml==6.0.3",
  "pytest==8.4.1",
  "ruff==0.12.5",
]
gpu = [
  "accelerate==1.14.0",
  "datasets==5.0.0",
  "huggingface-hub==1.24.0",
  "liger-kernel==0.8.1",
  "nvidia-modelopt[hf]==0.45.0",
  "peft==0.19.1",
  "transformers==5.9.0",
]

[project.scripts]
kova-campaign = "kova_training.cli:main"
kova-gpu-probe = "kova_training.topology:main"

[tool.hatch.build.targets.wheel]
packages = ["src/kova_training"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["kova_training"]
